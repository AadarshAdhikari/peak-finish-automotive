const reviewUrl='https://maps.app.goo.gl/haWqCKfQQN6azjuAA?g_st=ic';
const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function render(){const jobs=JSON.parse(localStorage.getItem('peakFinishOrders')||'[]'),root=document.querySelector('#jobs');if(!jobs.length){root.innerHTML='<div class="empty"><h2>No preview bookings yet</h2><p>Submit a request through the customer form to see it here.</p></div>';return}root.innerHTML=jobs.map((j,i)=>`<article class="job"><div class="job-top"><div><small>${esc(j.order)}</small><h3>${esc(j.customerName)} · ${esc(j.make)} ${esc(j.model)}</h3></div><strong class="status">${esc(j.status)}</strong></div><div class="job-grid"><div><span>Appointment</span><strong>${esc(j.date)} · ${esc(j.time)}</strong></div><div><span>Package</span><strong>${esc(j.package)}</strong></div><div><span>Payment</span><strong>${esc(j.payment)}</strong></div><div><span>Phone</span><strong>${esc(j.phone)}</strong></div><div><span>Address</span><strong>${esc(j.address)}</strong></div><div><span>Created</span><strong>${new Date(j.createdAt).toLocaleString('en-AU')}</strong></div></div><div class="job-actions"><button class="primary" data-action="complete" data-i="${i}">Mark completed</button><a class="secondary" href="tel:${esc(j.phone)}">Call</a><a class="secondary" href="sms:${esc(j.phone)}">Message</a>${j.status==='Completed'?`<a class="secondary" href="sms:${esc(j.phone)}?&body=${encodeURIComponent(`Hi ${j.customerName}, thank you for choosing Peak Finish Automotive for your ${j.make} ${j.model}. We hope you’re happy with the result! Your feedback helps our small local business grow. Please leave us a Google review: ${reviewUrl}`)}">Send review request</a>`:''}</div></article>`).join('')}
document.addEventListener('click',e=>{if(e.target.dataset.action==='complete'){const jobs=JSON.parse(localStorage.getItem('peakFinishOrders')||'[]');jobs[Number(e.target.dataset.i)].status='Completed';localStorage.setItem('peakFinishOrders',JSON.stringify(jobs));render()}});document.querySelector('#clearDemo').addEventListener('click',()=>{if(confirm('Remove all preview bookings from this browser?')){localStorage.removeItem('peakFinishOrders');render()}});render();
